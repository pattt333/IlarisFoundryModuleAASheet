{{!-- Biography Tab --}}
<div class="tab biography {{tab.cssClass}}" data-group="primary" data-tab="biography">
    <prose-mirror name="system.notes" value="{{ source.system.notes }}" toggled>{{{notesHTML}}}</prose-mirror>
</div>
